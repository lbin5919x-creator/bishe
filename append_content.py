#!/usr/bin/env python
# -*- coding: utf-8 -*-

content = """
## 三、改进方案

### 3.1 体现丁字路口和十字路口的差异

#### 方案1：优化车流模式设计

**丁字路口特点**：
- 缺少北向车流，应该强化南向车流的非对称性
- 东西向作为主干道，南向作为支路
- 建议设置潮汐车流（早晚高峰不同）

**十字路口特点**：
- 四个方向都有车流，体现对称性和复杂性
- 可以设置早晚高峰的不同流量模式
- 增加转向车流，体现路口复杂度

#### 方案2：调整固定时长配时方案

当前配置过于简单，建议根据路口特点优化：

**丁字路口**：
- 当前：[40, 40] - 两个相位均等
- 建议：[50, 30] - 主干道（东西向）50秒，支路（南向）30秒
- 理由：体现主干道优先的实际交通管理策略

**十字路口**：
- 当前：[30, 30, 30, 30] - 四个相位均等
- 建议：[35, 25, 35, 25] - 主要方向35秒，次要方向25秒
- 理由：体现主次干道的差异

### 3.2 体现单路口和多路口的差异

#### 方案1：增强多路口协同机制

修改协同参数，增强路口间的相互影响：
- coordination_weight: 从0.3提升到0.5
- link_queue_penalty: 从0.5提升到1.0

#### 方案2：增加训练回合数

多路口的动作空间更大（2^n），需要更多探索：
- 单路口：200回合
- 多路口：建议400回合（翻倍）

### 3.3 确保固定时长和DQN对比公平（最关键）

#### 方案1：统一随机种子

**这是最重要的改进！**

当前问题：seed默认为None，导致每次评估的车流都不同

解决方案：
1. 在评估时明确指定seed
2. 固定时长和DQN使用相同的seed序列

#### 方案2：增加评估回合数

从20回合增加到50回合，减少随机性影响

#### 方案3：创建对比评估脚本

确保两种控制器在完全相同的交通场景下进行对比

## 四、具体实施步骤

### 步骤1：修改固定时长配时方案（立即可做）

修改 config/settings.py：
```python
FIXED_TIME_PLANS = {
    "t_intersection": [50, 30],  # 主干道50s，支路30s
    "x_intersection": [35, 25, 35, 25],  # 主次干道差异化
    "cascaded_intersection": [35, 25, 35, 25],
}
```

### 步骤2：统一评估种子（立即可做，最重要）

修改 evaluation/evaluate.py：
```python
parser.add_argument("--seed", type=int, default=42,  # 改为默认42
                    help="SUMO仿真随机种子")
```

运行对比评估时：
```bash
# 固定时长
python evaluation/evaluate.py --scenario t_intersection --controller fixed_time --seed 42 --episodes 50

# DQN（使用相同seed）
python evaluation/evaluate.py --scenario t_intersection --controller dqn --seed 42 --episodes 50
```

### 步骤3：优化车流配置（需要重新训练）

修改 scenarios/t_intersection/routes.rou.xml 和 scenarios/x_intersection/routes.rou.xml
增加车流的差异性和复杂度

### 步骤4：增强多路口协同（需要重新训练）

修改 config/settings.py 中的 CascadedConfig 参数

### 步骤5：重新训练模型

使用新的配置重新训练所有场景的模型

## 五、预期效果

### 5.1 丁字路口 vs 十字路口

- 丁字路口：DQN学会给主干道更多绿灯时间
- 十字路口：DQN学会根据早晚高峰动态调整
- 性能差异：十字路口的优化空间更大，DQN优势更明显

### 5.2 单路口 vs 多路口

- 单路口：优化局部等待时间
- 多路口：优化全局通行效率，减少连接路拥堵
- 性能差异：多路口场景下DQN的协同优势更明显

### 5.3 固定时长 vs DQN

- 在相同车流下，DQN应该表现出10-20%的性能提升
- 等待时间减少、通行量增加
- 对突发车流（如优先车辆）响应更快

## 六、关键要点总结

1. **最关键的问题**：固定时长和DQN评估时没有使用相同的随机种子，导致车流不同，对比不公平

2. **立即可做的改进**：
   - 修改seed默认值为42
   - 调整固定时长配时方案
   - 增加评估回合数到50

3. **需要重新训练的改进**：
   - 优化车流配置
   - 增强多路口协同参数
   - 增加状态空间的拓扑感知

4. **评估建议**：
   - 使用相同seed序列（如42, 43, 44, ...）
   - 每个场景至少评估50个回合
   - 记录详细的性能指标（等待时间、通行量、排队长度）
"""

with open('问题分析与改进方案.md', 'a', encoding='utf-8') as f:
    f.write(content)

print("内容追加成功！")
